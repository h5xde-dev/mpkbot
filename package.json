{
  "name": "mpkbot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start:local": "vercel dev --yes",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "setup-webhook": "node --env-file=.env scripts/setup-webhook.mjs",
    "setup-webhook:prod": "PUBLIC_URL=https://mpkbot.vercel.app node --env-file=.env scripts/setup-webhook.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/node": "^5.1.8",
    "typescript": "^5.8.2",
    "vercel": "^54.9.1"
  }
}
